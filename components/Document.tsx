"use client";

function Document({ id }: { id: string }) {
  return (
    <div>
      <div>
        <form action=""></form>
      </div>
      <div>
        {/* ManageUsers */}

        {/* Avatars */}
      </div>

      {/* Collaborative editor */}
    </div>
  );
}

export default Document;
